const toUpperCase = require('/source/Skola/Assignement-6/main');

test("Hopefully this prints it out toUpperCase", () => {

    input = "hElLo WoRlD";
    output = "HELLO WORLD";

    expect(output).toBe("HELLO WORLD");
});